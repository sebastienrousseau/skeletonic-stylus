@charset "UTF-8"

@css {
  @layer skeletonic.base, skeletonic.layout, skeletonic.elements, skeletonic.components;
}

// Configurations (Variables & Colors)
@import "configurations/_contents.styl"

// Base
@layer skeletonic.base
  @import "base/_contents.styl"

// Layout
@layer skeletonic.layout
  @import "layout/_contents.styl"

// Elements
@layer skeletonic.elements
  @import "elements/_contents.styl"

// Components
@layer skeletonic.components
  @import "components/_contents.styl"
