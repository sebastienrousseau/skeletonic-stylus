/* @group Clipboard */

.bd-clipboard
  float: right
  position: relative

.button-clipboard
    display: block
    font-size: .65em
    min-width: auto
    padding: (round($gr, 1) - 1) rem
    position: absolute
    right: (round($gr, 1) + 1)rem
    top: round($gr, 1)rem
    z-index: 10

/* @end */
